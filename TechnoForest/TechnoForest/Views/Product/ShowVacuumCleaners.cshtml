@model TechnoForest.Models.Product.VacuumCleaners.ListVacuumCleanersViewModel

@{
    ViewData["Title"] = "ShowVacuumCleaners";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using System.Security.Claims;
<p style="color:white">Result: @Model.Vacuums.Count()</p>

<strong>
    <a asp-controller="Home" asp-action="Index" style="color: #0094ff">Go back</a>
</strong>

<table class="table table-hover">
    <thead>
        <tr>
            <th style="color:white" scope="col"></th>
            <th style="color:white" scope="col">Model</th>
            <th style="color:white" scope="col">Power</th>
            <th style="color:white" scope="col">Noise level</th>
            <th style="color:white" scope="col">Color</th>
            <th style="color:white" scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var vacuum in Model.Vacuums)
        {
            <tr>
                <td>
                    <img class="Phone" src="~/Images/VacuumCleanerShowRoom.png" />
                </td>
                <td style="color:white">@vacuum.ProductName - @vacuum.Model</td>
                <td style="color:white">@vacuum.Power</td>
                <td style="color:white">@vacuum.NoiseLevel dB</td>
                <td style="color:white">@vacuum.Color</td>
                <td style="color:white">@vacuum.Price</td>

                @if (User.Identity.IsAuthenticated)
                {
                    <td> <a asp-controller="Product" asp-action="AddFridgeToCart" asp-route-id="@vacuum.Id"><button style="background-color:#34495e" class="btn btn-primary">Аdded to cart</button></a></td>
                }
            </tr>
        }
    </tbody>
</table>
